1. Download using Git command 
2. Go to Project Folder
3. Open the terminal in that type "npm install"
4. after installing npm type "npm run dev"
5. after running the npm run dev you can see that the web-app is started in the localhost.
